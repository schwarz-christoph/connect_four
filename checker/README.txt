How-To:

Editieren Sie das Skript "checkers.ps1" und füllen Sie die Klassennamen und Pfade ihres Projektes ein.
Führen Sie anschließend das Skript aus dem Verzeichnis "\checker" aus.

Beispiel: PS C:\checker> .\chechers.ps1